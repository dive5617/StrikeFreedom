<Window x:Class="pBuild.Pnovol_Result_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pnovol_Result" Height="300" Width="600" Icon="pBuild.ico">
    <Grid>
        <DataGrid HeadersVisibility="Column" HorizontalGridLinesBrush="Transparent" VerticalGridLinesBrush="Transparent" AutoGenerateColumns="False" FrozenColumnCount="1" IsReadOnly="True"
                                SelectionMode="Extended" SelectionChanged="select_clk" RowHeaderWidth="0" ClipboardCopyMode="IncludeHeader" x:Name="pNovo_result_grid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding ID}" Width="50"/>
                <DataGridTextColumn Header="SQ" Binding="{Binding SQ}" Width="260" />
                <DataGridTextColumn Header="Modification" Binding="{Binding Modification_Str}" Width="180" />
                <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
